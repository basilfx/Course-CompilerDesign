(void)main:(String[])args {
	int a;

	[builtin print:@"Geef een getal, dan tel ik het keer twee"];
	a = [builtin read:a] * 2;

    while (a > 0) {
        [builtin print:a];
        a = a - 1;
    }
}